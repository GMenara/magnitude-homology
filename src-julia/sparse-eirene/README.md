The boundary_map_sparse.jl script builds the sparse matrix of the magnitude homology boundary operator along with two vectors dv and fv needed to compute homology using the package Eirene.
In the main_sparse.jl script a graph is chosen and the MH groups are computed.
